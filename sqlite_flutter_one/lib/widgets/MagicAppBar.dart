import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class CinephileAppBar {
  static AppBar build({BuildContext context, VoidCallback onCartPressed}) {
    return AppBar(title: Text('Cinephile'), actions: <Widget>[]);
  }
}
